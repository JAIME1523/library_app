import 'package:flutter/material.dart';

class SaveUserView extends StatelessWidget {
   
  const SaveUserView({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('SaveUserView'),);
  }
}